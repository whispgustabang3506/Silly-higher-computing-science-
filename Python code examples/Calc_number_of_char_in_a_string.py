#the length function, len(), counts the number of characters (including spaces) in a string 

sentence = "I never could get the hang of Thursdays"
print("There are", len(sentence),"characters in the sentence.")